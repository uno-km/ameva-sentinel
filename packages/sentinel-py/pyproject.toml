[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ameva-sentinel"
version = "2.1.0"
description = "Shadow-First Automation Risk Observation & Multi-Axis Threat Telemetry Layer for Python (FastAPI, Flask, Django)"
readme = "README.md"
authors = [{ name = "AMEVA Foundation", email = "zhfldk014745@naver.com" }]
license = { text = "Apache-2.0" }
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Security",
    "Topic :: Internet :: WWW/HTTP :: HTTP Servers",
]
keywords = [
    "security",
    "bot-detection",
    "threat-telemetry",
    "cloudflare-workers",
    "fastapi",
    "flask",
    "privacy-first"
]
requires-python = ">=3.8"
dependencies = []

[project.optional-dependencies]
fastapi = ["fastapi>=0.68.0", "starlette>=0.14.0"]
flask = ["flask>=2.0.0"]
all = ["fastapi>=0.68.0", "starlette>=0.14.0", "flask>=2.0.0"]
test = ["pytest>=7.0.0"]

[project.urls]
Homepage = "https://uno-km.vercel.app/sentinel/"
Documentation = "https://uno-km.vercel.app/lib/sentinel/"
Repository = "https://github.com/uno-km/ameva-sentinel"
Issues = "https://github.com/uno-km/ameva-sentinel/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["ameva_sentinel*"]
